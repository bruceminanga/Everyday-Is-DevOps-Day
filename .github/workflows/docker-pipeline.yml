name: Docker CI/CD Pipeline

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
  pull_request:
    branches: ["main"]

env:
  REGISTRY: ghcr.io

jobs:
  lint-build-test-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [backend, frontend] # Runs pipeline for both folders
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. Lint the specific service's Dockerfile
      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: ${{ matrix.service }}/Dockerfile
          failure-threshold: warning
          ignore: DL3006 # Ignores the ARG variable base-image false positive

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 2. Tag separate images (e.g., ghcr.io/user/repo-frontend and ghcr.io/user/repo-backend)
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-${{ matrix.service }}
          tags: |
            type=sha,format=short
            type=semver,pattern={{version}}

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 3. Build & Test locally
      - name: Build and export to local daemon
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}
          file: ./${{ matrix.service }}/Dockerfile
          load: true
          tags: test-${{ matrix.service }}:latest

      # 4. Service-Specific Smoke Test
      - name: Test Docker Image
        run: |
          if [ "${{ matrix.service }}" = "frontend" ]; then
            echo "Testing frontend Nginx syntax and config..."
            docker run --rm test-frontend:latest nginx -t
          elif [ "${{ matrix.service }}" = "backend" ]; then
            echo "Testing backend Python runtime and dependencies..."
            docker run --rm --entrypoint /usr/bin/python3 test-backend:latest -c "import sys; print('Backend Python runtime is healthy!')"
          fi

      # 5. Multi-Arch Push
      - name: Build and Push Multi-Arch Image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}
          file: ./${{ matrix.service }}/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
